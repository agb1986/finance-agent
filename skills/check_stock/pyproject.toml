[project]
name = "check-stock"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "common",
    "mplfinance>=0.12.10b0",
    "nbformat>=5.10.4",
    "plotext>=5.3.2",
    "yfinance>=1.2.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0",
    "pytest-cov>=7.0",
]
