[project]
name = "financial-news"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "common",
    "feedparser>=6.0.12",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
]
