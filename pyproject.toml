[project]
name = "finance-agent"
version = "0.1.0"
description = "Personal finance agent powered by Claude skills"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = ["common", "skills/*"]

[tool.uv.sources]
common = { workspace = true }
